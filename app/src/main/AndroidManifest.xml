<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.comp3004groupx.smartaccount">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.Transaction"></activity>
        <activity android:name=".view.Setting"></activity>
        <activity android:name=".view.Statistics"></activity>
        <activity android:name=".view.Account"></activity>
        <activity android:name=".view.Account_List"></activity>
        <activity android:name=".view.Add_Account"></activity>

        <activity android:name=".view.NewTransaction"></activity>

        <activity android:name=".view.Transaction_List_Account"></activity>
        <activity android:name=".view.Transaction_List_Date"></activity>
        <activity android:name=".view.Transaction_List_Type"></activity>
        <activity android:name=".view.View_Transaction"></activity>
    </application>

</manifest>